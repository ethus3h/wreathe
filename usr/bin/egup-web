#!/bin/bash
#Ember Git UPdater (and Web site updater)
#Need to do egup before build to ensure updates to futuramerlin-web-toolkit are committed before running it
egup
(
    cd /Ember\ Library/Ember/ember-web-site || exit 1; futuramerlin-web-toolkit-build
)
egup
